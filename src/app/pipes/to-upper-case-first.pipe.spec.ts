import { ToUpperCaseFirstPipe } from './to-upper-case-first.pipe';

describe('ToUpperCaseFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new ToUpperCaseFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
